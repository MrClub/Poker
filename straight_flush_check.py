# This will take a confirmed straight and a confirmed flush and compare them to see if they combine to be a straight flush.

import flush_check as flush
import straight_check as straigh

def straight_flush_check(confirmed_straight, confirmed_flush)